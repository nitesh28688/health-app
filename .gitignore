.env.local
.env
.env.vercel-*
node_modules/
.next/

# One-time bulk seed data — too large for git (branded CSVs exceed GitHub's
# 100MB/file limit), and disposable once seed-usda-branded.mjs has run.
data/usda_branded/
data/off_dump/
